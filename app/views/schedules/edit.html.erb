<h1>Editing schedule</h1>
<%= form_for(@schedule) do |f| %>
  <% if @schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% @schedule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :day_week, "Day of the week" %><br>
    <%= f.text_area :day_week, :disabled => "true" %>
  </div>
  <div class="field">
    <%= f.label :day_time, "Schedule" %><br>
<%= f.text_area :day_time, :disabled => "true" %>
</div>
  <div class="field">
    <%= f.label :temperature %><br>
    <%= f.number_field :temperature, :class => "textboxgrade" %>
  </div>
  <div class="field">
   
    <%= f.hidden_field :thermostat_id %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit :class => "btn btn-success" %>
  </div>
<% end %>
