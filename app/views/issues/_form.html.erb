<%= form_for(@issue) do |f| %>
  <% if @issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue.errors.count, "error") %> prohibited this issue from being saved:</h2>

      <ul>
      <% @issue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, :class => "textboxgrade", :required => "required" %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status, :class => "textboxgrade" %>
  </div>
  <div class="field">
    <%= f.label :resolution %><br>
    <%= f.text_area :resolution, :class => "textboxgrade" %>
  </div>
  <div class="field">
    <%= f.label :thermostat_id %><br>
    <%= f.text_field :thermostat_id, :class => "textboxgrade"%>
  </div>
  <br><br>
  <div class="actions">
    <%= f.submit :class => "btn btn-success"%>
  </div>
<% end %>
