<%= form_for(@alarm) do |f| %>
  <% if @alarm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alarm.errors.count, "error") %> prohibited this alarm from being saved:</h2>

      <ul>
      <% @alarm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  

  <div class="field">
    <%= f.label :number_range %><br>
    <%= f.number_field :number_range, :class => "textboxgrade", :required => "required" %>
  </div>
  <div class="field">
    <%= f.label :thermostat_id %><br>
    <%= f.number_field :thermostat_id, :disabled => "true"  %>
  </div>

  <br>
  <br>
  <div class="actions">
    <%= f.submit :class => "btn btn-success" %>
  </div>
<% end %>
