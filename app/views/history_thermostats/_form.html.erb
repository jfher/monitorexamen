<%= form_for(@history_thermostat) do |f| %>
  <% if @history_thermostat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history_thermostat.errors.count, "error") %> prohibited this history_thermostat from being saved:</h2>

      <ul>
      <% @history_thermostat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :temperature %><br>
    <%= f.number_field :temperature %>
  </div>
  <div class="field">
    <%= f.label :humidity %><br>
    <%= f.number_field :humidity %>
  </div>
  <div class="field">
    <%= f.label :energy %><br>
    <%= f.number_field :energy %>
  </div>
  <div class="field">
    <%= f.label :thermostat_id %><br>
    <%= f.text_field :thermostat_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
    <button type="submit" class="btn btn-success" ></button>
  </div>
<% end %>
